# 项目记忆点

请按照xx-xx-xx的方式记录每一天都做了什么

## 2026-08-28

- 完成 T5 第一版根因分析流程：新增模型客户端、规则候选根因、安全建议和模型失败回退。
- 新增 `src/model_client.py`、`src/analyze_root_cause.py`、`tests/test_root_cause.py`，并整理 T5 文档和 Prompt。
- 离线处理 20 个 T4 结果成功：14 个异常案例生成根因，6 个正常案例保持空根因和建议。
- 全部自动化测试通过，共 20 项；T5 单案例真实模型调用成功，模型返回根因和人工确认建议。
- 安全约束保持有效：不执行修复命令，不泄露 `expected_label`，不记录 API Key。
- 当前进度：T0-T6 已完成；下一步为 T7，统计准确率和平均响应时间。

## 2026-08-29

- 计划继续检查前端没有显示指标的问题，准备核对 `web/app.js` 的数据加载、`eval/*.json` 数据文件和本地网页服务。
- 本次尚未修改项目文件；首次检查命令因执行环境工作目录无效而未完成。
- 下次继续确认前端是否读取 `eval/cases.json` 等真实数据，并用浏览器强制刷新验证指标展示。
